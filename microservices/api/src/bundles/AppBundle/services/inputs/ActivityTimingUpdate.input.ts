import { Schema, Is, a, an } from "@bluelibs/validator-bundle";
import { ActivityTimingUpdateInput as BaseActivityTimingUpdateInput } from "./ActivityTimingUpdate.input.base";

@Schema()
export class ActivityTimingUpdateInput extends BaseActivityTimingUpdateInput {
  // You can extend the base here
}
