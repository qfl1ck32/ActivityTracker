import { S3UploadService } from "@bluelibs/x-s3-bundle";
import { container } from "@root/__tests__/ecosystem";
import { mockImage } from "./utilities/files";

// TODO: make sure it works; atm: ACLs error
describe("test", () => {
  //   test("idk", async () => {
  //     const s3UploaService = container.get(S3UploadService);
  //     const upload = await s3UploaService.upload(mockImage);
  //     console.log(upload);
  //   }, 50000);
});
