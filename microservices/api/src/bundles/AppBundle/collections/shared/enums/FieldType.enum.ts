export enum FieldType {
  BOOLEAN = "BOOLEAN",
  ENUM = "ENUM",
  NUMBER = "NUMBER",
  STRING = "STRING",
  OBJECT = "OBJECT",
}

export enum FieldTypeLabels {
  "BOOLEAN" = "Boolean",
  "ENUM" = "Enum",
  "NUMBER" = "Number",
  "STRING" = "String",
  "OBJECT" = "Object",
}
