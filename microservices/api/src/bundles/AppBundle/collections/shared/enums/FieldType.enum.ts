export enum FieldType {
  BOOLEAN = "BOOLEAN",
  ENUM = "ENUM",
  DATE = "DATE",
  STRING = "STRING",
}

export enum FieldTypeLabels {
  "BOOLEAN" = "Boolean",
  "ENUM" = "Enum",
  "DATE" = "Date",
  "STRING" = "String",
}
