export enum FieldType {
  BOOLEAN = "BOOLEAN",
  ENUM = "ENUM",
  NUMBER = "NUMBER",
  STRING = "STRING",
}

export enum FieldTypeLabels {
  "BOOLEAN" = "Boolean",
  "ENUM" = "Enum",
  "NUMBER" = "Number",
  "STRING" = "String",
}
