export enum FieldType {
  BOOLEAN = "BOOLEAN",
  ENUM = "ENUM",
  INTEGER = "INTEGER",
  FLOAT = "FLOAT",
  STRING = "STRING",
}

export enum FieldTypeLabels {
  "BOOLEAN" = "Boolean",
  "ENUM" = "Enum",
  "INTEGER" = "Integer",
  "FLOAT" = "Float",
  "STRING" = "String",
}
